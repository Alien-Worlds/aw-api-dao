const defaultStartBlock = -1;
const defaultEndBlock = 0xffffffff;
const defaultTestBlock = 0;
const defaultReplayMode = false;

module.exports = {
    defaultStartBlock,
    defaultEndBlock,
    defaultTestBlock,
    defaultReplayMode,
};
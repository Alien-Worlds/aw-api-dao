export enum MSIGStateIndex {
  PENDING = 0,
  EXECUTED = 1,
  CANCELLED = 2,
}

export enum MSIGStateKey {
  PENDING = 'pending',
  EXECUTED = 'executed',
  CANCELLED = 'cancelled',
  UNKNOWN = 'unknown',
}
